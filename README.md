# git-table
Paste a GitHub link, list images, preview them, reorder/sort list, delete with undo, download selected preview, and generate a thumbnail table gallery + HTML code. TABLE preview uses placeholder thumbnails.
